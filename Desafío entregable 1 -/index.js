class productManager{
    constructor (products = []){
    this.title = nombreProducto,
    this.description = descripcionProducto,
    this.price = precio,
    this.thumbnail = rutaImagen,
    this.code = codigoIdentificador,
    this.stock = piezasDisponible
}  
}

addProduct (nuevoProducto); {
    products.push(nuevoProducto)
let id= parseInt(document.getElementById('id_subcat').value);
function x(){
    id++;
    return id;
    }
}

getProducts(); {
    return products.lenght;
}

getProductById(); {
let products = document.getProductById ("products");
}